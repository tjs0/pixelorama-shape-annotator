class_name GlobalProjectStub
extends RefCounted
## Compile-time stub standing in for Pixelorama's `Project` type, exposing only
## the members the extension reads off `Global.current_project`. EXCLUDED from
## the exported pack; the real Project class is provided by Pixelorama at runtime.

var frames: Array = []
var current_frame: int = 0
